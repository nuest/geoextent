#!/usr/bin/env python
"""Setup script for geoextent.

This minimal setup.py is required for editable installs (pip install -e .)
when using setuptools-scm with older setuptools versions.

The actual configuration is in pyproject.toml.
"""

from setuptools import setup

setup()
